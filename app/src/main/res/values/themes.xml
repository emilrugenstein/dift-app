<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all real theming (ui/theme/Theme.kt); this only styles the window frame. -->
    <style name="Theme.Dift" parent="android:Theme.Material.NoActionBar" />
</resources>
